CREATE MEMORY TABLE IF NOT EXISTS customer (
	id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY, 
	firstname VARCHAR(256) NOT NULL, 
	lastname VARCHAR(256) NOT NULL, 
	birthdate DATE NOT NULL
	);

